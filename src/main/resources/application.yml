server:
  port: 8082

spring:
  application:
    name: employee-service
  cloud:
    consul:
      host: localhost
      port: 8500
  devtools:
    restart:
      enabled: true
  datasource:
    url: jdbc:sqlserver://l4g.database.windows.net:1433;database=employee-practice
    username: l4gadmin@l4g
    password: "#DDjjL4G"
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update